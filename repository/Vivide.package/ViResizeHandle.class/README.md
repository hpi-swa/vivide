A handle to resize morphs in width or height according to its orientation. Automatically considers if it is left or right (resp. above or below) the target morph and calls appropriate geometry functions.